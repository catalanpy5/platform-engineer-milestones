# Remote state (recommended). Create the bucket first, then uncomment and
# run `terraform init -reconfigure`.
#
# terraform {
#   backend "gcs" {
#     bucket = "TODO-tfstate-bucket"
#     prefix = "istio-microservices"
#   }
# }
